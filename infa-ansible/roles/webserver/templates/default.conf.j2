server {
    listen {{ webserver_port }} default_server;
    server_name _;

    root /var/www/site2;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    # Bloquer les fichiers cachés
    location ~ /\. {
        deny all;
    }
}
