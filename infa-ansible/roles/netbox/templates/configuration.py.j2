ALLOWED_HOSTS = ['{{ netbox_listen_address }}', 'localhost']

CSRF_TRUSTED_ORIGINS = [
    'https://{{ netbox_listen_address }}',
    'https://localhost:8443',
    'http://localhost:8443',
]

DATABASE = {
    'NAME': 'netbox',
    'USER': 'netbox',
    'PASSWORD': '{{ netbox_db_password }}',
    'HOST': 'localhost',
    'PORT': '',
    'CONN_MAX_AGE': 300,
}

REDIS = {
    'tasks': {
        'HOST': 'localhost',
        'PORT': 6379,
        'SSL': False,
    },
    'caching': {
        'HOST': 'localhost',
        'PORT': 6379,
        'SSL': False,
    }
}

SECRET_KEY = '{{ netbox_secret_key }}'

STATIC_ROOT = '/opt/netbox/netbox/static'
